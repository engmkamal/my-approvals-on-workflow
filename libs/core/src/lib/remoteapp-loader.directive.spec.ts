import { RemoteappLoaderDirective } from './remoteapp-loader.directive';

describe('RemoteappLoaderDirective', () => {
  it('should create an instance', () => {
    const directive = new RemoteappLoaderDirective();
    expect(directive).toBeTruthy();
  });
});
